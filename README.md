# bgpRouteServer
BGP Route Server with database support to use with black hole routing etc. Script can be modified to advertise IPV4 prefixes, or alter to get VPN4 prefixes rd,rt or next-hop from the database.

- Used osrg/gobgp bgp library for bgp session
- Used mysql for database connection

